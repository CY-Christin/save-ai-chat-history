{
  "name": "ai-chat-notion-sync",
  "version": "0.0.1",
  "description": "Archive AI conversations to Notion. Browser extension.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@tryfabric/martian": "^1.2.4"
  }
}
