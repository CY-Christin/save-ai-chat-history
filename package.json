{
  "name": "save-ai-chat-history",
  "version": "0.0.1",
  "description": "Save AI Chat History — browser extension that archives AI conversations to Notion or Cloudflare R2.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node package.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@tryfabric/martian": "^1.2.4"
  }
}
